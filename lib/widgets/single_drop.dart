import 'package:flutter/material.dart';

class SingleDrop extends StatelessWidget {
  const SingleDrop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
